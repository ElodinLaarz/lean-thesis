import QuadraticOrder.Prime

/-!
# Layer 2b: Canonical Form for Ideals of QuadraticOrder

Every ideal of `QuadraticOrder d` can be written in a canonical `ℤ`-basis
form `⟨a, b + τ⟩` with `a > 0`, `0 ≤ b < a`, and `a | N(b + τ)`.

This is the key structural lemma underlying the ideal-counting results.

## Main results (TODO)

* `ideal_canonical_form` — every ideal has a canonical basis
* `canonical_form_unique` — the canonical form is unique
-/

namespace QuadraticOrder

-- TODO: Layer 2b implementation

end QuadraticOrder
