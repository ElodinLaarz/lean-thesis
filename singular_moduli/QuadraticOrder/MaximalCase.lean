import QuadraticOrder.InvertibleCount

/-!
# Layer 5a: The Maximal Order Case

When `d ≡ 0` or `1 (mod 4)`, `QuadraticOrder d` is the full ring of integers
of `ℚ(√d)` (a maximal order). In this case the ideal-counting formula
specialises to the classical Dedekind zeta function formula.

## Main results (TODO)

* `isMaximalOrder_iff` — characterisation of when `QuadraticOrder d` is maximal
* `idealCount_maximal` — specialised ideal count for maximal orders
-/

namespace QuadraticOrder

-- TODO: Layer 5a implementation

end QuadraticOrder
