import QuadraticOrder.Basic

/-!
# Layer 3: Root Counting for Quadratic Congruences

This file counts solutions to `x² ≡ c (mod pⁿ)` — the analytic input
needed for the ideal-counting theorems. The key result is a formula for
`#{x : ZMod (p^n) | x^2 = c}` in terms of the Legendre symbol `(c / p)`.

## Main results (TODO)

* `rootCount_prime_sq` — number of roots mod `p^n` for a prime `p`
* `rootCount_mul` — multiplicativity in `n`
-/

namespace QuadraticOrder

-- TODO: Layer 3 implementation

end QuadraticOrder
