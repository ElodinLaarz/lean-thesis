import QuadraticOrder.CanonicalForm
import QuadraticOrder.RootCounting

/-!
# Layer 4a: Ideal Counting — Theorem 3.1.2

This file proves Geiger's Theorem 3.1.2: a formula for the number of ideals
of norm `n` in `QuadraticOrder d`.

## Main results (TODO)

* `idealCount_eq` — `#{I : Ideal (QuadraticOrder d) | N(I) = n} = Σ_{a | n} (d/a)`
  where `(d/a)` is the Kronecker symbol.
-/

namespace QuadraticOrder

-- TODO: Layer 4a implementation (Theorem 3.1.2)

end QuadraticOrder
